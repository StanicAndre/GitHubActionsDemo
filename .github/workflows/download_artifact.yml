name: Download data

on:
  workflow_run:
    workflows: [Upload data]
    types:
      - completed

jobs:
  download:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        name: checkout
        
      - id: download_artifact
        name: download artifact
        shell: powershell
        run: |
          .\powershell\download_artifact.ps1 -github_token ${{ secrets.GITHUB_TOKEN }}   
          
      - id: copy_file
        name: copy file
        shell: powershell
        run: |
          $Session = New-PSSession -ComputerName "MD3ZPJ8C" -Credential "AD001\z003pksp"
          Copy-Item "$github.repository\math-homework.txt" -Destination "C:\Users\z003pksp\Downloads" -FromSession $Session
      
